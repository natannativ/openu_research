# תיאור מערך הניסוי — ASRT + נדידת קשב + n-back חזותי

מסמך זה מסכם את מבנה הניסוי, המשתנים, מספר הצעדים בכל תנאי, התזמון, וקבצי הפלט.

---

## 1. מטרת המחקר

בדיקה האם אנשים לומדים דפוסים סטטיסטיים נסתרים במיקומים (למידה אימפליצ. / ASRT)
**ללא מודעות**, וכיצד **עומס קוגניטיבי** (מטלת n-back חזותית במקביל) ונטייה ל**נדידת קשב**
משפיעים על למידה זו.

---

## 2. שלוש מטלות במקביל

1. **ASRT** — מטלת זמן תגובה טורית מתחלפת. בכל ניסיון מופיע עיגול לבן באחד מ-4 מיקומים;
   על הנבדק/ת ללחוץ על המקש המתאים מהר ובדיוק.
2. **n-back חזותי** — אות מופיעה במרכז המסך בכל ניסיון (העומס הקוגניטיבי).
3. **בדיקות נדידת קשב (thought probes)** — 3 שאלות קצרות אחרי כל בלוק.

בנוסף, בסוף הניסוי נשאלת **שאלת מודעות** אחת (האם הבחנת בדפוס).

---

## 3. משתנים בלתי תלויים

### בין-נבדקים: תנאי עומס (`condition`)
| תנאי | `n_back_level` | מספר צעדים | מה הנבדק/ת עושה עם האות |
|------|----------------|------------|--------------------------|
| `low_load`  | 1 | **1-back** | לוחץ/ת **רווח** כשהאות זהה לאות שהופיעה בניסיון **הקודם** |
| `high_load` | 2 | **2-back** | לוחץ/ת **רווח** כשהאות זהה לאות שהופיעה **שני ניסיונות אחורה** |

> "מספר הצעדים" = רמת ה-n-back. כרגע: 1 בתנאי הקל, 2 בתנאי הכבד. ניתן לשנות בקובץ אחד.
> בשני התנאים יש מטלת n-back פעילה, כך שהמניפולציה היא עומס נמוך (1) מול גבוה (2).

### בתוך-נבדקים
- **סוג טריפלט** (`triplet_type`): המדד המרכזי ללמידה — השוואת זמני תגובה בין
  טריפלטים בעלי-סבירות-גבוהה (High) לנמוכה (Low).
- **בלוק** (`block`): התקדמות הלמידה לאורך זמן (1–20).

---

## 4. משתנים תלויים (מדדים)

- **זמן תגובה (RT) ודיוק** ב-ASRT (`asrt_rt`, `asrt_correct`).
- **אפקט הלמידה הסטטיסטית** = הפרש ה-RT בין טריפלטים Low ל-High (על ניסיונות נכונים).
- **ביצועי n-back**: זיהויים (hits), החמצות (misses), אזעקות שווא (false alarms).
- **נדידת קשב**: שלוש שאלות אחרי כל בלוק (`probe_focus`, `probe_content`, `probe_spontaneous`).
- **מודעות לדפוס**: שאלת כן/לא בסיום (`awareness_response`).

---

## 5. מבנה הניסוי

### בלוק תרגול (לא נשמר)
- **40 ניסיונות** במיקומים אקראיים (בגרסה הקצרה: 20).
- בשני התנאים (1-back ו-2-back) — **פידבק n-back מיידי** אחרי כל ניסיון
  (זיהוי נכון / החמצה / לחיצה מיותרת), כדי שהנבדק/ת ילמד/תלמד את המטלה.
  **בניסוי עצמו אין פידבק פר-ניסיון.**

### הסבר לשאלות נדידת הקשב (לפני המטלה האמיתית)
- מסכי הסבר קצרים על שלוש שאלות הנדידה, כולל דוגמאות קצרות.
- אין חידון תרגול, כדי לא להעמיס על המשתתפים לפני המטלה.

### הניסוי עצמו
- **20 בלוקים × 32 ניסיונות = 640 ניסיונות** (~27 דק'; גרסה קצרה לבדיקות: 1 בלוק × 16).
- כל בלוק: רצף ASRT מתחלף — ניסיון ראשון אקראי (lead-in), ואז לסירוגין
  **P (דפוס)** ו-**R (אקראי)**. בכל בלוק הדפוס חוזר 4 פעמים (4 מיקומים).
- לניתוח: 5 בלוקים = אפוך אחד (4 אפוכים).
- אות n-back בכל ניסיון (6 אותיות A–F). **חזרות (targets) מתרחשות רק בניסיונות
  אקראיים (R)**, כך שמטלת האות אינה משבשת את זמני התגובה בשלשות הדפוס.
- **אכיפת מטלת האות**: אם הייתה חזרה (target) והנבדק/ת לא לחצ/ה רווח (החמצה) —
  מוצג חיווי אדום ולא ניתן להמשיך עד לחיצת מקש. הצלחות וניסיונות ללא חזרה ממשיכים רגיל.
  הניסיון *שאחרי* החיווי מסומן ב-`after_letter_test=1` (מומלץ להחריג אותו מניתוח ה-RT).
- אחרי כל בלוק: 3 שאלות נדידת קשב + מסך פידבק ביצועים (ממתין ללחיצה).
- בשני התנאים: אם שיעור הזיהוי ב-n-back בבלוק יורד מתחת ל-50% — מוצג מסך תזכורת.

### סיום
- שאלת מודעות אחת (Y/N) → נשמרת בקובץ session.

---

## 6. תזמון (timing)

| שלב | משך |
|-----|-----|
| השהיה לפני הניסיון הראשון בבלוק (`INITIAL_DELAY`) | 1000 ms |
| מרווח בין ניסיונות (RSI, `ITI`) | 120 ms |
| הופעת המטרה (העיגול) | נשארת עד **תגובה נכונה** (correction loop) |
| חלון תגובת n-back (`NBACK_RESPONSE_WINDOW`) | **1200 ms קבוע** מהופעת האות |

**correction loop**: לחיצה שגויה ב-ASRT משאירה את המטרה על המסך עד הלחיצה הנכונה.
הניקוד מתבסס על **התגובה הראשונה**. תקרת בטיחות: 15 ניסיונות.

---

## 7. איזון (counterbalancing)

קיימים **24 רצפי דפוס** (כל התמורות של [1,2,3,4]). הרצף נבחר לפי **מספר הנבדק/ת**
(`participant`) באופן דטרמיניסטי וניתן לשחזור — ראו עמודה `asrt_sequence`.

---

## 8. מקשי תגובה

| מיקום | מקש | תיאור |
|-------|-----|-------|
| 1 | S | שמאל חיצוני |
| 2 | F | שמאל פנימי |
| 3 | J | ימין פנימי |
| 4 | L | ימין חיצוני |
| n-back | SPACE | חזרה על האות (1-back) |

---

## 9. קבצי פלט

לכל נבדק/ת נוצרים שני קבצי CSV (קידוד UTF-8):

1. `data_<participant>_<condition>_<timestamp>.csv` — **שורה אחת לכל ניסיון** (long format, מוכן לניתוח).
2. `session_<participant>_<condition>_<timestamp>.csv` — שורה אחת לכל נבדק/ת (דמוגרפיה + מודעות).

### מילון משתנים — קובץ הניסיונות (data)

| עמודה | משמעות |
|-------|--------|
| `participant` | מזהה נבדק/ת |
| `condition` | `low_load` / `high_load` |
| `n_back_level` | 1 (low) או 2 (high) |
| `asrt_sequence` | רצף הדפוס שנבחר לנבדק/ת (למשל "3241") |
| `block` | מספר בלוק (1–20) |
| `epoch` | אפוך (1–5), קבוצה של 4 בלוקים = `ceil(block/4)` |
| `trial_in_block` | מספר ניסיון בתוך הבלוק |
| `global_trial` | מספר ניסיון רץ לאורך כל הניסוי |
| `p_or_r` | `P` = ניסיון דפוס, `R` = ניסיון אקראי |
| `asrt_position` | מיקום המטרה (1–4) |
| `asrt_correct_key` | המקש הנכון (s/f/j/l) |
| `asrt_response` | המקש שנלחץ (תגובה ראשונה) |
| `asrt_correct` | 1 = נכון, 0 = שגוי (לפי התגובה הראשונה) |
| `asrt_rt` | זמן תגובה **בשניות** של התגובה הראשונה |
| `asrt_n_attempts` | מספר לחיצות עד התגובה הנכונה (correction loop) |
| `nback_letter` | האות שהוצגה |
| `nback_target` | 1 = האות חוזרת לפי רמת ה-n-back (target; רק בניסיונות R), 0 = לא |
| `nback_response` | 1 = נלחץ רווח, 0 = לא |
| `nback_correct` | 1 = התגובה תאמה ל-target (כולל אי-לחיצה נכונה) |
| `after_letter_test` | 1 = הניסיון מגיע מיד אחרי חיווי החמצת אות (מומלץ להחריג מ-RT) |
| `triplet` | שלושת המיקומים האחרונים (למשל "3-1-2") |
| `triplet_type` | H=high, L=low, R=repetition, T=trill, X=לא נכלל |
| `probe_focus` | תשובת שאלת ריכוז (1–4) — זהה לכל שורות הבלוק |
| `probe_content` | תוכן המחשבות (1 = לא חשבתי על כלום / mind-blanking … 4 = חשבתי על משהו מסוים). דיכוטומיה: 1–2 = MB, 3–4 = MW עם תוכן |
| `probe_spontaneous` | ספונטני↔מכוון (1 = ספונטני לחלוטין … 4 = מכוון לחלוטין). דיכוטומיה: 1–2 = ספונטני, 3–4 = מכוון |

> הערות לניתוח:
> - לניתוח RT של למידה: לסנן `asrt_correct == 1` ולהשוות `triplet_type` H מול L.
> - טריפלטים מסווגים רק על בסיס ניסיונות שנענו **נכון** (כמו ב-vekteo); `X` = לא נכלל.
> - `asrt_rt` בשניות — להכפיל ב-1000 ל-ms.

### מילון משתנים — קובץ ה-session

`participant, age, gender, adhd, condition, n_back_level, asrt_sequence, timestamp, awareness_response, awareness_detail`
(`awareness_detail`: טקסט חופשי — מה שהנבדק/ת תיאר/ה כשענה/תה "כן" לשאלת המודעות)
(`awareness_response`: y = הבחין/ה בדפוס, n = לא).

---

## 10. פרמטרים מרכזיים לשינוי מהיר (בראש `main.py`)

`N_BLOCKS`, `N_SEQUENCE_REPS`, `N_PRACTICE_TRIALS`, `ITI`, `INITIAL_DELAY`,
`NBACK_RESPONSE_WINDOW`, `NBACK_TARGET_RATE`, `NBACK_WARNING_THRESHOLD`,
ורמות ה-n-back לכל תנאי (`n_back_level`).
