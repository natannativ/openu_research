# Python dependencies for the ASRT + Mind-Wandering + Visual n-back experiment.
#
# RECOMMENDED for the lab machine: install the "Standalone PsychoPy" application
# instead of using pip (https://www.psychopy.org/download.html). It bundles a
# known-good Python + all libraries and avoids dependency/timing surprises.
# Use this requirements.txt only if you run main.py from your own Python env.
#
# Validated with the versions below (latest as of 2026-06). Pin these for the
# duration of data collection; do not upgrade mid-study.

psychopy==2026.1.3
python-bidi==0.6.10
